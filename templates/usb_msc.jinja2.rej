diff a/templates/usb_msc.jinja2 b/templates/usb_msc.jinja2	(rejected hunks)
@@ -27,7 +27,6 @@ struct InterfaceWrapper
   struct Interface *pipe;
   struct Pin rx;
   struct Pin tx;
-  bool inversion;
 };
 /*----------------------------------------------------------------------------*/
 static enum Result interfaceInit(void *, const void *);
@@ -61,10 +60,9 @@ static enum Result interfaceInit(void *object, const void *configBase)
   interface->pipe = config->pipe;
 
   interface->rx = pinInit(config->rx);
-  assert(pinValid(interface->rx));
   pinOutput(interface->rx, BOARD_LED_INV);
+
   interface->tx = pinInit(config->tx);
-  assert(pinValid(interface->tx));
   pinOutput(interface->tx, BOARD_LED_INV);
 
   return E_OK;
