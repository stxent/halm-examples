/*
 * {{group.name}}/{{group.alias}}/main.c
 * Automatically generated file
 */

#include "board.h"
#include <assert.h>
/*----------------------------------------------------------------------------*/
{%- if config.MEMORY_SIZE is defined %}
#define MEMORY_SIZE {{config.MEMORY_SIZE}}
{%- else %}
#define MEMORY_SIZE 65536
{%- endif %}

typedef uint32_t TestWord;
/*----------------------------------------------------------------------------*/
static TestWord patternOnes(size_t)
{
  return (TestWord)0xFFFFFFFFUL;
}
/*----------------------------------------------------------------------------*/
static TestWord patternOnesEven(size_t)
{
  return (TestWord)0x55555555UL;
}
/*----------------------------------------------------------------------------*/
static TestWord patternOnesOdd(size_t)
{
  return (TestWord)0xAAAAAAAAUL;
}
/*----------------------------------------------------------------------------*/
static TestWord patternSequential(size_t position)
{
  return (TestWord)position;
}
/*----------------------------------------------------------------------------*/
static TestWord patternZeros(size_t)
{
  return (TestWord)0x00000000UL;
}
/*----------------------------------------------------------------------------*/
static bool test(void *base, size_t size, TestWord (*pattern)(size_t))
{
  const size_t elements = size / sizeof(TestWord);
  TestWord * const address = base;
  bool passed = true;

  for (size_t position = 0; position < elements; ++position)
    address[position] = pattern(position);

  for (size_t position = 0; position < elements; ++position)
  {
    const TestWord value = address[position];

    if (value != pattern(position))
      passed = false;
  }

  return passed;
}
/*----------------------------------------------------------------------------*/
int main(void)
{
  boardSetupClockPll();

  const struct Pin led = pinInit(BOARD_LED);
  pinOutput(led, BOARD_LED_INV);

{%- if config.MEMORY_SUFFIX is defined %}

  void * const address = boardSetupMemory{{config.MEMORY_SUFFIX}}();
{%- else %}

  void * const address = boardSetupMemory();
{%- endif %}

  while (1)
  {
    bool passed;

    /* Fill with all ones */
    passed = test(address, MEMORY_SIZE, patternOnes);
    pinWrite(led, passed ? BOARD_LED_INV : !BOARD_LED_INV);

    /* Fill with all zeros */
    passed = test(address, MEMORY_SIZE, patternZeros);
    pinWrite(led, passed ? BOARD_LED_INV : !BOARD_LED_INV);

    /* Fill with 0xAA pattern */
    passed = test(address, MEMORY_SIZE, patternOnesOdd);
    pinWrite(led, passed ? BOARD_LED_INV : !BOARD_LED_INV);

    /* Fill with 0x55 pattern */
    passed = test(address, MEMORY_SIZE, patternOnesEven);
    pinWrite(led, passed ? BOARD_LED_INV : !BOARD_LED_INV);

    /* Fill with pseudorandom pattern */
    passed = test(address, MEMORY_SIZE, patternSequential);
    pinWrite(led, passed ? BOARD_LED_INV : !BOARD_LED_INV);
  }

  return 0;
}
